[tool.pytest.ini_options]
testpaths = ["packages/flycore/tests", "services/brain/tests"]
pythonpath = ["services/brain"]
markers = [
    "slow: deep perft and full-connectome builds",
    "data: needs the FlyWire CSVs in data/raw/",
]
