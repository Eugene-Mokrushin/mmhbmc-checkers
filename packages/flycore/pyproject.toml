[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "flycore"
version = "0.1.0"
description = "Checkers rules shared by the brain and web services"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["flycore"]
package-dir = { "flycore" = "." }
